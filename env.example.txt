# =========================================
# Discipline Society · Variables de entorno
# =========================================
# Copia este archivo a .env en local (netlify dev lo lee automáticamente)
# y configura los MISMOS valores en Netlify:
#   Site settings → Environment variables
#
# NUNCA subas el .env real al repo.
# =========================================

# --- Stripe ---
# dashboard.stripe.com → Developers → API keys
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxx

# dashboard.stripe.com → Products → Founders Hoodie → (Price ID)
STRIPE_HOODIE_PRICE_ID=price_xxxxxxxxxxxxxxxxxxxxxx

# URL pública de tu sitio (sin barra al final)
SITE_URL=https://disciplinesociety.com

# --- Resend (opcional — emails de bienvenida) ---
# Si lo dejas vacío, el formulario seguirá funcionando:
# los emails se guardarán en Netlify → Forms.
RESEND_API_KEY=
FROM_EMAIL=Discipline Society <hola@tudominio.com>
ADMIN_EMAIL=tu@email.com
